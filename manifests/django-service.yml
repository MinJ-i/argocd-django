apiVersion: v1
kind: Service
metadata:
  name: django-service
spec:
  type: NodePort
  selector:
    app.kubernetes.io/name: web-deploy
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
      nodePort: 32345   # ✅ 새 포트 (32235 대신)
